<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li style="list-style-type: none">
      <%= link_to restaurant.name, restaurant_path(restaurant), style: 'text-decoration: none' %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add a new restaurant', new_restaurant_path %>
